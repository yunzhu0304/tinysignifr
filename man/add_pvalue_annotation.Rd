% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_pvalue_annotation.R
\name{add_pvalue_annotation}
\alias{add_pvalue_annotation}
\title{Add p-value annotations to a non-faceted ggplot}
\usage{
add_pvalue_annotation(
  plot_data,
  stat_data,
  x,
  y,
  label = "p.adj.signif",
  hide.ns = FALSE,
  signif.cutoff = 0.05,
  expand = 0.12,
  step.increase = 0.08,
  size = 6,
  bracket.size = 1,
  tip.length = 0,
  ...,
  x_levels = NULL
)
}
\arguments{
\item{plot_data}{A data frame (or object coercible to one) containing the
raw data used for plotting. Must include the columns named by \code{x} and \code{y}.}

\item{stat_data}{A data frame (or object coercible to one) containing
pairwise comparison results. Must contain columns \code{group1}, \code{group2}, and
the column specified by \code{label}. Typically the \verb{@stat} slot from
\code{\link[tinystatr:stat3]{tinystatr::stat3()}} or \code{\link[tinystatr:stat2]{tinystatr::stat2()}}.}

\item{x}{A single character string giving the name of the discrete grouping
variable (x-axis) in \code{plot_data}.}

\item{y}{A single character string giving the name of the numeric response
variable (y-axis) in \code{plot_data}.}

\item{label}{A single character string giving the name of the column in
\code{stat_data} that contains significance labels (e.g. \code{"*"}, \code{"**"},
\code{"ns"}). Defaults to \code{"p.adj.signif"}.}

\item{hide.ns}{Controls removal of non-significant rows before bracket
positions are computed. Removing non-significant rows before computing
positions prevents large empty gaps in the y-axis. Accepted values:
\itemize{
\item \code{FALSE} (default): keep all rows.
\item \code{TRUE}: auto-detect a suitable significance column and remove rows
labelled \code{"ns"} or rows with numeric values above \code{signif.cutoff}.
\item \code{"p"}: filter using a p-value column.
\item \code{"p.adj"}: filter using an adjusted p-value column.
\item Any other character string: treated as the name of the column to
use for filtering.
}}

\item{signif.cutoff}{A single non-negative finite numeric value giving the
threshold used when filtering numeric significance columns via \code{hide.ns}.
Defaults to \code{0.05}.}

\item{expand}{A single non-negative numeric value controlling the gap between
the top of the data and the first bracket, expressed as a fraction of the
y-axis range. Defaults to \code{0.12}.}

\item{step.increase}{A single non-negative numeric value controlling the
vertical distance between successive brackets, expressed as a fraction of
the y-axis range. Defaults to \code{0.08}.}

\item{size}{Font size for significance labels, passed to
\code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}. Defaults to \code{6}.}

\item{bracket.size}{Line width of the significance brackets. Defaults to \code{1}.}

\item{tip.length}{Length of the bracket tips. Defaults to \code{0}.}

\item{...}{Additional arguments passed to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}},
such as \code{fontface = "bold"}.}

\item{x_levels}{An optional character vector explicitly specifying the order
of groups on the x-axis. When \code{NULL} (default), the order is inferred from
\code{plot_data[[x]]}: factor levels are used when available, otherwise groups
are sorted alphabetically.}
}
\value{
A ggplot2 layer generated by \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}, or
\code{NULL} if no valid comparisons remain after filtering.
}
\description{
Add significance brackets and labels to a single-panel \code{ggplot2} object.
Bracket y-positions are computed automatically from the data, so no manual
\code{y.position} column is required in \code{stat_data}.
}
\details{
\code{stat_data} is typically the \verb{@stat} slot extracted from the S4 object
returned by \code{\link[tinystatr:stat3]{tinystatr::stat3()}} or \code{\link[tinystatr:stat2]{tinystatr::stat2()}}.

Bracket y-positions are computed by an internal helper that sorts comparisons
by span (number of groups spanned) and stacks brackets upward with a minimum
step of \code{step.increase * y_range}. Non-significant rows are removed
\strong{before} position computation to avoid empty vertical gaps.
}
\examples{
\dontrun{
library(ggplot2)
library(tinystatr)
library(tinysignifr)

data("ToothGrowth")
ToothGrowth$dose <- factor(ToothGrowth$dose)

stat_result <- stat3(
  data = ToothGrowth,
  group = "dose",
  value = "len",
  formula = len ~ dose
)

ggplot(ToothGrowth, aes(x = dose, y = len)) +
  geom_boxplot(aes(color = dose), fill = NA) +
  theme_classic() +
  theme(legend.position = "none") +
  add_pvalue_annotation(
    plot_data = ToothGrowth,
    stat_data = stat_result@stat,
    x = "dose",
    y = "len",
    hide.ns = TRUE
  )
}

}
\seealso{
\code{\link[=add_pvalue_mixed]{add_pvalue_mixed()}}, \code{\link[=add_pvalue_facet_annotation]{add_pvalue_facet_annotation()}},
\code{\link[=add_cross_facet_annotation]{add_cross_facet_annotation()}}, \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}
}
