% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_pvalue_mixed.R
\name{add_pvalue_mixed}
\alias{add_pvalue_mixed}
\title{Add p-value brackets with automatic size adjustment for mixed symbol types}
\usage{
add_pvalue_mixed(
  plot_data,
  stat_data,
  x,
  y,
  label = "p.adj.signif",
  hide.ns = FALSE,
  signif.cutoff = 0.05,
  expand = 0.12,
  step.increase = 0.08,
  size = 6,
  bracket.size = 1,
  tip.length = 0,
  special_symbols = NULL,
  special_size_ratio = 0.65,
  ...,
  x_levels = NULL
)
}
\arguments{
\item{plot_data}{A data frame (or object coercible to one) containing the
raw data used for plotting. Must include the columns named by \code{x} and \code{y}.}

\item{stat_data}{A data frame (or object coercible to one) containing
pairwise comparison results. Must contain columns \code{group1}, \code{group2}, and
the column specified by \code{label}. Typically the \verb{@stat} slot from
\code{\link[tinystatr:stat3]{tinystatr::stat3()}} or \code{\link[tinystatr:stat2]{tinystatr::stat2()}}.}

\item{x}{A single character string giving the name of the discrete grouping
variable (x-axis) in \code{plot_data}.}

\item{y}{A single character string giving the name of the numeric response
variable (y-axis) in \code{plot_data}.}

\item{label}{A single character string giving the name of the column in
\code{stat_data} that contains significance labels. Defaults to
\code{"p.adj.signif"}.}

\item{hide.ns}{Controls removal of non-significant rows before bracket
positions are computed. See \code{\link[=add_pvalue_annotation]{add_pvalue_annotation()}} for accepted values.}

\item{signif.cutoff}{A single non-negative finite numeric value used as the
threshold when filtering numeric significance columns via \code{hide.ns}.
Defaults to \code{0.05}.}

\item{expand}{A single non-negative numeric value controlling the gap between
the top of the data and the first bracket (fraction of y range). Defaults
to \code{0.12}.}

\item{step.increase}{A single non-negative numeric value controlling the
vertical step between successive brackets (fraction of y range). Defaults
to \code{0.08}.}

\item{size}{Font size for significance labels. Defaults to \code{6}.}

\item{bracket.size}{Line width of the significance brackets. Defaults to \code{1}.}

\item{tip.length}{Length of the bracket tips. Defaults to \code{0}.}

\item{special_symbols}{A character vector of label prefixes that should be
rendered at a reduced font size (see \code{special_size_ratio}). A label is
treated as special if it starts with any of the listed prefixes.
Accepted values:
\itemize{
\item \code{NULL} (default): only \code{"#"} is treated as special.
\item A character vector such as \code{c("#", "$", "\%", "@", "&")}.
\item \code{NA} or \code{character(0)}: disables special handling; all labels are
rendered at the same size.
}}

\item{special_size_ratio}{A single numeric value in \verb{(0, 1]} giving the
scaling factor applied to \code{size} for special-symbol labels. Defaults to
\code{0.65}.}

\item{...}{Additional arguments passed to \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}},
such as \code{fontface = "bold"}.}

\item{x_levels}{An optional character vector specifying the order of groups
on the x-axis. When \code{NULL} (default), the order is inferred from
\code{plot_data[[x]]}.}
}
\value{
A ggplot2 layer, or a list of two layers when both normal and special
labels are present, that can be added to a \code{ggplot} object with \code{+}.
Returns \code{NULL} when there are no brackets to draw after filtering.
}
\description{
A wrapper around \code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}} that automatically computes
bracket y-positions and renders user-defined \emph{special} significance symbols
(e.g. \code{"#"}, \code{"$"}) at a reduced font size so that they appear visually
consistent with standard star symbols (\code{"*"}, \code{"**"}, etc.).
}
\details{
This is useful when \code{stat_data} contains labels from different symbol
systems, such as \code{"*"} from one test and \code{"#"} from another.

When \code{special_symbols} is active, \code{stat_data} is split into two subsets and
\code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}} is called twice: once at full \code{size} for
standard labels and once at \code{size * special_size_ratio} for special-symbol
labels. Both subsets share the same y-positions computed from the filtered
\code{stat_data}.
}
\examples{
\dontrun{
library(ggplot2)
library(tinystatr)
library(tinysignifr)

data("ToothGrowth")
ToothGrowth$dose <- factor(ToothGrowth$dose)

stat_df <- stat3(
  data = ToothGrowth,
  group = "dose",
  value = "len",
  formula = len ~ dose
)@stat

stat_df$p.adj.signif[3] <- "#"

ggplot(ToothGrowth, aes(x = dose, y = len)) +
  geom_boxplot(aes(color = dose), fill = NA) +
  theme_classic() +
  theme(legend.position = "none") +
  add_pvalue_mixed(
    plot_data = ToothGrowth,
    stat_data = stat_df,
    x = "dose",
    y = "len",
    hide.ns = TRUE,
    special_symbols = "#",
    special_size_ratio = 0.65
  )
}

}
\seealso{
\code{\link[=add_pvalue_annotation]{add_pvalue_annotation()}}, \code{\link[=add_pvalue_facet_annotation]{add_pvalue_facet_annotation()}},
\code{\link[ggpubr:stat_pvalue_manual]{ggpubr::stat_pvalue_manual()}}
}
